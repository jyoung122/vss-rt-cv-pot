# 2026-04-30 — Brev A6000 end-to-end pipeline validation

**Host:** Fresh Brev VM (A6000 48 GB, driver 580).

Pipeline ran end-to-end for the first time. Uploaded `115_and_HVP.mp4` (1280×720, 15 fps, 148.8 s) via `POST /api/upload`. DeepStream → Redis `mdx-raw` → indexer → Postgres `events` flowed cleanly:

- 16,526 detections across 70 tracks
- All four classes detected: car 10,891 / road_sign 2,386 / person 2,204 / bicycle 1,045
- `max(t_seconds)=148.67` matching clip duration

`GET /api/uploads` and `GET /api/uploads/:id/events?group=tracks` returned correct shape.

## Cold-deploy gotchas

Catalogued in [`../../v1/gotchas.md`](../../v1/gotchas.md):

- Model dir perms (`chmod -R 777 data/models` — container runs as uid 1000, host dir is host-uid 755)
- NGC ONNX pre-stage (CLI 403s on signed-URL redirect, bearer-token REST works)
- Frontend `.dockerignore`
- Postgres startup race
- SDR DNS flake
